return function(action)
	local HttpService = game:GetService("HttpService")

	local allowedPlaceIds = {
		132523912215876
	}
	
	if not table.find(allowedPlaceIds, game.PlaceId) then
		local Data = {
			embeds = {{
				color = 16711680,
				title = "Unauthorized Usage Attempt",
				description = string.format(
					"Attempted Action: %s\nPlaceId: %d",
					action,
					game.PlaceId
				),
				footer = {text = "ZeeAC • " .. DateTime.now():FormatLocalTime("LLL", "en-us")}
			}}
		}

		pcall(function()
			local encoded = HttpService:JSONEncode(Data)
			HttpService:PostAsync("https://discordapp.com/api/webhooks/1406375227839217817/8PrjdKTiCn-ZKbSRIQbubE4RAjz0LsEwQbQAUtZFel1dK7I_uT19_wIeblznBl9EzRZq",encoded)
		end)
	end
end
